spring:
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update # Change to 'validate' in production
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

# JWT Settings
application:
  security:
    jwt:
      secret-key: ${JWT_SECRET:Q2h6TzB0dU5Yb0JmNXJ2aWl2VUdQWnN5c2hQWkFJc3Z5dQ==}
      expiration: 86400000 # 24 Hours

# Razorpay Settings (Get these from dashboard.razorpay.com)
razorpay:
  key:
    id: ${RAZORPAY_KEY_ID:rzp_test_SC07BKUJDCqEO4}
    secret: ${RAZORPAY_SECRET:QXD6GMmX17qSsDQ1W0OZ3Rbo}